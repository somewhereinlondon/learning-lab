package datastructures;

public class Big0Ex1 {

    public static void main(String[] args) {
        // Task 1 — Big O analysis (answers only, no runnable code)
        // A: O(1)   — direct index access
        // B: O(n)   — single loop
        // C: O(n²)  — nested loops
        // D: O(a+b) — two separate loops on different inputs
        // E: O(1)   — three direct index accesses on fixed size array
    }
}